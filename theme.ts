import { DefaultTheme } from 'styled-components';

const mainThemme: DefaultTheme = {
  colors: {
    white: '#ffffff',
  },
  sizes: {
    1: '0.2rem',
  },
};

const themes = {
  main: mainThemme,
};

export default themes;
